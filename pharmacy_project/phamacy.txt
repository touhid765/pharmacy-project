CREATE DATABASE pharmacy;

USE pharmacy;


CREATE TABLE inventory (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    stock_in INT DEFAULT 0,
    stock_out INT DEFAULT 0,
    expired INT DEFAULT 0
);

CREATE TABLE sales (
    id INT AUTO_INCREMENT PRIMARY KEY,
    date DATE NOT NULL,
    customer_id VARCHAR(50) NOT NULL,
    medicine_name VARCHAR(100) NOT NULL,
    quantity INT NOT NULL,
    selling_price DECIMAL(10, 2) NOT NULL,
    total_amount DECIMAL(10, 2) NOT NULL
);

CREATE TABLE customers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone_number VARCHAR(15) NOT NULL,
    purchased_medicines TEXT NOT NULL,
    discount DECIMAL(10, 2),
    total_amount DECIMAL(10, 2),
    paid_status ENUM('Paid', 'Unpaid') NOT NULL
);

CREATE TABLE pharmacists (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    timetable VARCHAR(255) NOT NULL,
    attendance BOOLEAN DEFAULT FALSE
);

INSERT INTO pharmacists (username, password, timetable, attendance)
VALUES
('touhid1', 't1', 'Monday - Friday: 9 AM - 5 PM', FALSE),
('touhid2', 't2', 'Monday - Friday: 10 AM - 6 PM', FALSE),
('touhid3', 't3', 'Tuesday - Saturday: 8 AM - 4 PM', FALSE),
('touhid4', 't4', 'Wednesday - Sunday: 11 AM - 7 PM', FALSE),
('admin', 'adminpass', 'Admin has no timetable.', FALSE);

CREATE TABLE transactions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    pharmacist_id INT NOT NULL,
    date DATE NOT NULL,
    details VARCHAR(255) NOT NULL,
    amount DECIMAL(10, 2) NOT NULL,
    status ENUM('Pending', 'Approved') DEFAULT 'Pending',
    FOREIGN KEY (pharmacist_id) REFERENCES pharmacists(id)
);
